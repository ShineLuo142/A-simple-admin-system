<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>新闻分类</title>
</head>
<body>
<!--The content below is only a placeholder and can be replaced.-->

<link rel="stylesheet" href="/bootstrap/css/bootstrap.css">


<!--公共头部开始-->
<%include ../public/header.ejs%>
<!--公共头部结束-->



<div class="container-fluid">
    <div class="row">
        <div class="col-sm-2">
            <%include ../public/aside.ejs%>

        </div>
        <div class="col-sm-10">
            <ol class="breadcrumb">
                <li class="active">新闻分类列表管理
                </li>
                <li class="active">新闻分类列表
                </li>
            </ol>
            <a href="/admin/newscate/add" class="btn btn-primary" style="margin-bottom: 20px">增加新闻分类</a>

            <div class="panel panel-default">
                <div class="panel-heading">
                    搜索
                </div>
                <div class="panel-body">
                    <form role="form" class="form-inline">
                        <div class="form-group">
                            <label for="name">名称</label>
                            <input type="text" class="form-control" id="name" placeholder="请输入名称">
                        </div>

                        <div class="form-group">
                            <button type="submit" class="btn btn-default">开始搜索</button>
                        </div>
                    </form>
                </div>
            </div>
            <!--
                列表展示
            -->
            <div class="table-responsive">
                <table class="table table-striped text-center">
                    <thead>
                    <tr>
                        <th class="text-center">列表名称</th>
                        <th class="text-center">状态</th>
                        <th class="text-center">描述</th>
                        <th class="text-center">操作</th>
                    </tr>
                    </thead>
                    <tbody>

                    <%for(var i=0;i<list.length;i++){%>
                        <tr>
                            <td><%=list[i].title%></td>
                            <td><%if(list[i].status==0){%>
                                    在线
                                <%}else if(list[i].status==1){%>
                                    <p style="color: red">过期</p>
                                <%}%></td>
                            <td><%=list[i].description%></td>
                            <td><a href="/admin/newscate/edit?id=<%=list[i]._id%>">编辑</a>　　<a href="/admin/newscate/delate?id=<%=list[i]._id%>">删除</a></td>
                        </tr>
                    <%}%>

                    </tbody>
                </table>
            </div>

        </div>
    </div>
</div>

</body>
</html>